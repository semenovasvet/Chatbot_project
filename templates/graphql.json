{
    "query": "query ($myquery:String) { orders(first: 10, query: $myquery) { edges { node { id } } } }",
    "order": "query ($id: ID!){ order(id: $id) { id name email fulfillments { trackingInfo { url number } } } }"
}